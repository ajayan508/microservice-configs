#datasource specific properties
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cartdb
    username: root
    password: root1234#		

#jpa specific properties
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


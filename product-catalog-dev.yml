#datasource specific properties
spring:
  datasource:
    url: jdbc:mysql://mysql-container:3306/shipkartdb
    username: root
    password: root

#jpa specific properties
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

message: in development profile  - On editing the config, we can refresh the configuration
